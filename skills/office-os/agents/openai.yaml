interface:
  display_name: "Office OS"
  short_description: "Run local Office work from plain-language requests"
  default_prompt: "Use $office-os to update my local Office files efficiently."
policy:
  allow_implicit_invocation: true
